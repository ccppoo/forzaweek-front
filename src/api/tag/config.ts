import { API_HOST, ContentTypeHeader } from '@/api/config';

export const BASE_PATH_TAG = `${API_HOST}/tag/tag`;
export const BASE_PATH_TAGS = `${API_HOST}/tag/tags`;
export const BASE_PATH_TAGGING = `${API_HOST}/tag/tagging`;
export const BASE_PATH_TAG_CATEGORY = `${API_HOST}/tag/category`;

export const JSONContentType = ContentTypeHeader.JSON;
